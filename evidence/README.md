# evidence/

Command / terminal output saved as `.txt` (text beats a photo of a terminal — it's
searchable and reads as more legit). Drop any supporting exports here, e.g. a
`Connect-MgGraph` context dump or a Conditional Access **What If** result you'd rather
keep as text than a screenshot.

> The Phase 2 provisioning transcripts live in
> [`../Provisioning/sample-logs/`](../Provisioning/sample-logs/), not here.
